pre-commit:
  parallel: true
  jobs:
    - run: bun oxlint --type-aware --type-check {staged_files}
      root: "apps/example"
      glob: "**/*.{js,ts,jsx,tsx}"
    - run: bun eslint --max-warnings=0 --no-warn-ignored {staged_files}
      root: "apps/example"
      glob: "**/*.{js,ts,jsx,tsx}"
