pre-commit:
  parallel: true
  jobs:
    - run: pnpm tsc
      root: "apps/example"
      glob: "**/*.{js,ts,jsx,tsx}"
    - run: pnpm oxlint --type-aware {staged_files}
      root: "apps/example"
      glob: "**/*.{js,ts,jsx,tsx}"
    - run: pnpm eslint {staged_files}
      root: "apps/example"
      glob: "**/*.{js,ts,jsx,tsx}"
