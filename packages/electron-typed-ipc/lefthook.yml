pre-commit:
  parallel: true
  jobs:
    - run: pnpm tsc
      root: "packages/electron-typed-ipc"
      glob: "**/*.{js,ts,jsx,tsx}"
    - run: pnpm oxlint --type-aware {staged_files}
      root: "packages/electron-typed-ipc"
      glob: "**/*.{js,ts,jsx,tsx}"
