extends:
  - apps/*/lefthook.yml
  - packages/*/lefthook.yml

pre-commit:
  jobs:
    - run: bun oxfmt {staged_files}
      glob: "**/*.{js,ts,jsx,tsx,json,jsonc,yaml,yml,html,css}"
      stage_fixed: true

pre-push:
  parallel: true
  jobs:
    - run: bun lint
      glob: "**/*.*"
